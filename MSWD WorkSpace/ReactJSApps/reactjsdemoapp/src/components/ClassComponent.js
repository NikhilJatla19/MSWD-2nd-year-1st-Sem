import React, {Component} from 'react'

class ClassComponent extends React.Component
{
    render()
    {
        return <div>
            <h1>Class Components Demo (ClassComponent.js)</h1>
            <p>Class components extends Component base class from React and it gives way to the react life cycle methods and add a render function which is use for returning the react elements. A class component requires you to extend from React. Component and create a render function which returns a React element.</p>
        </div>
    }
}
export default ClassComponent